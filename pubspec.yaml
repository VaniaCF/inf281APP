name: inf281_app
description: "Aplicación para edificio inteligente"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # DEPENDENCIAS NECESARIAS PARA EL LOGIN
  http: ^1.1.0 # Para llamadas HTTP a tu API
  provider: ^6.1.1 # Para manejo de estado
  shared_preferences: ^2.2.2  # ✅ AGREGADO - PARA GUARDAR SESIONES

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true